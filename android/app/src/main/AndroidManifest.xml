<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    
    <!-- ========================================== -->
    <!-- Разрешения                                  -->
    <!-- ========================================== -->
    
    <!-- Интернет (обязательно) -->
    <uses-permission android:name="android.permission.INTERNET" />
    
    <!-- Камера (для QR и фото логотипа) -->
    <uses-permission android:name="android.permission.CAMERA"/>
    
    <!-- Фотогалерея (старые версии Android) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="32"/>
    
    <!-- Фотогалерея (Android 13+) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO"/>
    
    <!-- Геолокация (для карты ресторанов) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    
    <!-- Биометрия (отпечаток пальца / Face Unlock) -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />

    <!-- Минимальная версия Android -->
    <uses-sdk android:minSdkVersion="21" />

    <application
        android:label="Foot Food"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="false"
        android:allowBackup="false">
        
        <!-- 
        КРИТИЧНО для безопасности:
        - usesCleartextTraffic="false" = только HTTPS, HTTP запрещен
        - allowBackup="false" = запрет резервного копирования через ADB
          (защищает токены и пароли от кражи через бэкап)
        -->
        
        <!-- Конфигурация безопасности сети -->
        <meta-data
            android:name="android.security.net.config"
            android:resource="@xml/network_security_config" />
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            
            <!-- Deep Links -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="footfood" />
            </intent-filter>
        </activity>
        
        <!-- Flutter -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    
    <!-- Required to query activities that can process text -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>